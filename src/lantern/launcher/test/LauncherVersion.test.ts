import { fetchMinecraftManifest } from "../LauncherVersion";

describe("LauncherVersion", () => {
  describe("fetchMinecraftManifest", () => {
    it(`Fetch and response`, async () => {
      // let _response = await fetchMinecraftManifest();
      // _response.data.on("data", (chunk) => {
      //   console.log(chunk);
      // });
    });
  });
});
